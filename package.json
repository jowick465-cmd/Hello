{
  "name": "cicd-vite-hello",
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "lint": "eslint src/",
    "test": "echo 'Vite app — visual testing' && exit 0",
    "start": "npx serve dist -l 3000",
    "manager": "node src/docker-manager.cjs"
  },
  "dependencies": {},
  "devDependencies": {
    "vite": "^5.4.0",
    "eslint": "^9.0.0"
  }
}
